import React from 'react';
import { render } from '@testing-library/react';
// import Deckbuilder from '../Deckbuilder';

describe('<Deckbuilder>', () => {
  test('it should render <Deckbuilder>', () => {
    // render(<Deckbuilder/>);
    render(<div/>)
    expect(true).toBeTruthy()
  });
});
