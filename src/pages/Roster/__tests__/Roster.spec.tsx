import React from 'react';
import { render } from '@testing-library/react';
// import Roster from '../Roster';

describe('<Roster>', () => {
  test('it should render <Roster>', () => {
    // render(<Roster/>);
    render(<div/>)
    expect(true).toBeTruthy()
  });
});
