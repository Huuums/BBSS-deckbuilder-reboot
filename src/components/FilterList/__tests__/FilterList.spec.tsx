import React from 'react';
import { render } from '@testing-library/react';
// import FilterList from '../FilterList';

describe('<FilterList>', () => {
  test('it should render <FilterList>', () => {
    // render(<FilterList/>);
    render(<div/>)
    expect(true).toBeTruthy()
  });
});
