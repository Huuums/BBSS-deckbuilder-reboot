import React from 'react';
import { render } from '@testing-library/react';
// import RadioButton from '../RadioButton';

describe('<RadioButton>', () => {
  test('it should render <RadioButton>', () => {
    // render(<RadioButton/>);
    render(<div/>)
    expect(true).toBeTruthy()
  });
});
