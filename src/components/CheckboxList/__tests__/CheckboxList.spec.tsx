import React from 'react';
import { render } from '@testing-library/react';
// import CheckboxList from '../CheckboxList';

describe('<CheckboxList>', () => {
  test('it should render <CheckboxList>', () => {
    // render(<CheckboxList/>);
    render(<div/>)
    expect(true).toBeTruthy()
  });
});
