import React from 'react';
import { render } from '@testing-library/react';
// import Checkbox from '../Checkbox';

describe('<Checkbox>', () => {
  test('it should render <Checkbox>', () => {
    // render(<Checkbox/>);
    render(<div/>)
    expect(true).toBeTruthy()
  });
});
