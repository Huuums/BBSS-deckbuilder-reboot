import React from 'react';
import { render } from '@testing-library/react';
// import TrainerType from '../TrainerType';

describe('<TrainerType>', () => {
  test('it should render <TrainerType>', () => {
    // render(<TrainerType/>);
    render(<div/>)
    expect(true).toBeTruthy()
  });
});
