import React from 'react';
import { render } from '@testing-library/react';
// import DeckDisplay from '../DeckDisplay';

describe('<DeckDisplay>', () => {
  test('it should render <DeckDisplay>', () => {
    // render(<DeckDisplay/>);
    render(<div/>)
    expect(true).toBeTruthy()
  });
});
