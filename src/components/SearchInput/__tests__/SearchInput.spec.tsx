import React from 'react';
import { render } from '@testing-library/react';
// import SearchInput from '../SearchInput';

describe('<SearchInput>', () => {
  test('it should render <SearchInput>', () => {
    // render(<SearchInput/>);
    render(<div/>)
    expect(true).toBeTruthy()
  });
});
