import React from 'react';
import { render } from '@testing-library/react';
// import Combobox from '../Combobox';

describe('<Combobox>', () => {
  test('it should render <Combobox>', () => {
    // render(<Combobox/>);
    render(<div/>)
    expect(true).toBeTruthy()
  });
});
