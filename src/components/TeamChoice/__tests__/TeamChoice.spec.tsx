import React from 'react';
import { render } from '@testing-library/react';
// import TeamChoice from '../TeamChoice';

describe('<TeamChoice>', () => {
  test('it should render <TeamChoice>', () => {
    // render(<TeamChoice/>);
    render(<div/>)
    expect(true).toBeTruthy()
  });
});
