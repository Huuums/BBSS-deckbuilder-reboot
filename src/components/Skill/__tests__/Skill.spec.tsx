import React from 'react';
import { render } from '@testing-library/react';
// import Skill from '../Skill';

describe('<Skill>', () => {
  test('it should render <Skill>', () => {
    // render(<Skill/>);
    render(<div/>)
    expect(true).toBeTruthy()
  });
});
