import React from 'react';
import { render } from '@testing-library/react';
// import AuthProvider from '../AuthProvider';

describe('<AuthProvider>', () => {
  test('it should render <AuthProvider>', () => {
    // render(<AuthProvider/>);
    render(<div/>)
    expect(true).toBeTruthy()
  });
});
