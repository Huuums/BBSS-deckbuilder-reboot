import React from 'react';
import { render } from '@testing-library/react';
// import SkillDisplay from '../SkillDisplay';

describe('<SkillDisplay>', () => {
  test('it should render <SkillDisplay>', () => {
    // render(<SkillDisplay/>);
    render(<div/>)
    expect(true).toBeTruthy()
  });
});
