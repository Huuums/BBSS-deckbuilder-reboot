import React from 'react';
import { render } from '@testing-library/react';
// import Trainerlist from '../Trainerlist';

describe('<Trainerlist>', () => {
  test('it should render <Trainerlist>', () => {
    // render(<Trainerlist/>);
    render(<div/>)
    expect(true).toBeTruthy()
  });
});
